node_modules
.next
out
build
.env
.env.local
.env*.local
*.tsbuildinfo
.DS_Store
.vercel

# Internal handbook. Contains live admin credentials, so it must never
# reach the public repository. Share it directly instead.
docs/handbook.html
docs/*.pdf
